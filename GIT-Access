The atheme.org GIT repository is available for anonymous read-only 
access.

To retrieve the audacious-plugins development branch from git.atheme.org, do:
   $ git clone git://git.atheme.org/audacious-plugins.git audacious-plugins-devel

To retrieve the audacious-plugins stable branch from git.atheme.org, do:
   $ git clone git://git.atheme.org/release/audacious-plugins-$VERSION.git

   Where $VERSION is the stable major version, for example the latest
   fixes and updates to 2.5-series of audacious-plugins would be in:

   $ git clone git://git.atheme.org/release/audacious-plugins-2.5.git
